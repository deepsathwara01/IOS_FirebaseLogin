# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'signinUsing google' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for signinUsing google

pod 'Firebase/Auth'
pod 'GoogleSignIn'

  target 'signinUsing googleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'signinUsing googleUITests' do
    # Pods for testing
  end

end
